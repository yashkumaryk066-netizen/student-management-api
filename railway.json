{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn manufatures.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/students/",
    "healthcheckTimeout": 100
  }
}
